#pragma once

// ============================================================
// Vec2i - simple 2D integer vector (map coordinates)
// Used for all grid-based positioning.
// ============================================================
struct Vec2i {
    int r = 0;  // row
    int c = 0;  // column
};
